# Pip configuration for ML Mini-Project
# Place this in your project root or pip config directory

[global]
timeout = 60
trusted-host = pypi.org
               pypi.python.org
               files.pythonhosted.org

[install]
upgrade-strategy = only-if-needed
prefer-binary = true